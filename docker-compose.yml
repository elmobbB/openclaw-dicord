version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
  bot:
    build: .
    command: ["python", "discord_bot.py"]
    env_file:
      - .env
    depends_on:
      - api
