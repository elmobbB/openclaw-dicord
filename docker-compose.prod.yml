version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
  bot:
    build: .
    command: ["python", "discord_bot.py"]
    env_file:
      - .env
    depends_on:
      - api
    restart: unless-stopped
